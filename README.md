GUI-App-to-Publish-Xenapp
=========================

Script to Publish application using GUI mode


The scripts creates a GUI app to publish application in a farm. The script needs to be run from server, member of same farm.

